<?xml version="1.0" encoding="utf-8"?>

<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Color de fondo -->
    <solid android:color="@color/blanco"/>

    <!-- Borde -->
    <stroke
        android:width="2dp"
        android:color="@color/gris_bg"/>

    <!-- Esquinas redondeadas -->
    <corners android:radius="12dp"/>

</shape>